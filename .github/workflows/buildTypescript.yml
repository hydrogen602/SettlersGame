name: Build Typescript

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build typescript
      run:
        echo "building..."
        tsc
        echo "done"
        
